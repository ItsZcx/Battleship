/*
** EPITECH PROJECT, 2023
** public_github
** File description:
** clean_bin
*/

#include "lib.h"

void clean_bin(char *bin)
{
    int index = 0;

    while (bin[index] != '\0') {
        bin[index] == '\0';
        index++;
    }
}
